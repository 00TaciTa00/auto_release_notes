/** 플랫폼 표시 레이블 — Dashboard, RepoSettings 공용 */
export const PLATFORM_LABEL: Record<string, string> = {
  gitlab: 'GitLab',
  github: 'GitHub',
}

/** diff 소스 표시 레이블 — Dashboard, RepoSettings 공용 */
export const DIFF_SOURCE_LABEL: Record<string, string> = {
  api: 'REST API',
  'local-git': '로컬 git',
}
